[package]
name = "lau-symplectic-agent"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-symplectic-agent"
description = "Agents as Hamiltonian systems — symplectic phase space, structure-preserving decisions, guaranteed behavioral invariants"

[dependencies]
nalgebra = "0.33"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
approx = "0.5"
